apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.dataApi.name }}
  labels:
    app: {{ .Values.services.dataApi.name }}
spec:
  # Using NodePort to ensure external access for the UI App
  type: NodePort 
  selector:
    app: {{ .Values.services.dataApi.name }}
  ports:
    - port: {{ .Values.services.dataApi.serverPort }}
      targetPort: {{ .Values.services.dataApi.serverPort }}
      protocol: TCP
      nodePort: {{ .Values.services.dataApi.nodePort }}
