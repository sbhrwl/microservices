apiVersion: v1
kind: Service
metadata:
  name: {{ .Values.services.storageService.name }}-service
  labels:
    app: {{ .Values.services.storageService.name }}
spec:
  type: NodePort
  selector:
    app: {{ .Values.services.storageService.name }}
  ports:
    - name: http
      port: {{ .Values.services.storageService.httpPort }}
      targetPort: {{ .Values.services.storageService.httpPort }}
      nodePort: 30{{ .Values.services.storageService.httpPort | printf "%03d" }}
    - name: grpc
      port: {{ .Values.services.storageService.grpcPort }}
      targetPort: {{ .Values.services.storageService.grpcPort }}
