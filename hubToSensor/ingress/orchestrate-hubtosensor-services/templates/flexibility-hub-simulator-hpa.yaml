{{- if .Values.flexibilityHubSimulator.hpa.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Values.flexibilityHubSimulator.name }}-hpa
  namespace: {{ .Release.Namespace }}
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Values.flexibilityHubSimulator.name }}-deployment
  minReplicas: {{ .Values.flexibilityHubSimulator.hpa.minReplicas }}
  maxReplicas: {{ .Values.flexibilityHubSimulator.hpa.maxReplicas }}
  metrics:
  - type: Resource
    resource:
      name: cpu
      target:
        type: Utilization
        averageUtilization: {{ .Values.flexibilityHubSimulator.hpa.cpuTarget }}
  - type: Resource
    resource:
      name: memory
      target:
        type: Utilization
        averageUtilization: {{ .Values.flexibilityHubSimulator.hpa.memoryTarget }}
{{- end }}
