spring:
  application:
    name: sensor-backend-service
  
  data:
    mongodb:
      host: localhost
      port: 27017
      database: sensordb
      username: root
      password: root123
      authentication-database: admin

grpc:
  server:
    port: 9090

logging:
  level:
    com.example.sensor: DEBUG
    org.springframework.data.mongodb: DEBUG