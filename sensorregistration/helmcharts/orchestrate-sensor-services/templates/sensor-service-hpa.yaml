apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ include "microservices.fullname" . }}-sensor-hpa
  labels:
    app: sensor-service
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ include "microservices.fullname" . }}-sensor-service
  minReplicas: {{ .Values.hpa.sensor.minReplicas }}
  maxReplicas: {{ .Values.hpa.sensor.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.sensor.targetCPUUtilizationPercentage }}
