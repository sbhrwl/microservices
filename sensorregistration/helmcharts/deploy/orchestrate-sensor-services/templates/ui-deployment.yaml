apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Chart.Name }}-{{ 'uiService' }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Chart.Name }}-{{ 'uiService' }}
  template:
    metadata:
      labels:
        app: {{ .Chart.Name }}-{{ 'uiService' }}
    spec:
      containers:
        - name: {{ 'uiService' }}
          image: {{ .Values.uiService.image }}
          ports:
            - containerPort: {{ .Values.uiService.port }}
          env:
            {{- range $key, $value := .Values.uiService.env }}
            - name: {{ $key }}
              value: {{ $value | quote }}
            {{- end }}
