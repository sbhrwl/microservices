{{- if .Values.registrationService.autoscaling.enabled }}
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: {{ .Chart.Name }}-registrationService
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: {{ .Chart.Name }}-registrationService
  minReplicas: {{ .Values.registrationService.autoscaling.minReplicas }}
  maxReplicas: {{ .Values.registrationService.autoscaling.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.registrationService.autoscaling.targetCPUUtilizationPercentage }}
{{- end }}
