import java.util.Scanner;

class ScannerDemo {
    public static void main(String[] args) {
        System.out.println("Hello, World!");
        // 1. Write a program to print the value of input if it is even and divisible by
        System.out.println("Hello, World! provide valid input");
        Scanner scan = new Scanner(System.in);
        int a = scan.nextInt();
        if (a % 2 == 0 || a % 3 == 0) {
            System.out.println("Hello, World! correct input");
        } else {
            System.out.println("Hello, World! Incorrect input");
        }
        // 2. Write a short program that gives the following as output -
        // - For each multiple of 3, print "Fizz" instead of the number.
        // - For each multiple of 5, print "Buzz" instead of the number.
        // - For numbers which are multiples of both 3 and 5, print "FizzBuzz" instead
        // of the number. Otherwise print the number itself.
        // 3. Write a short program that prints each number from 1 to 100 on a new line,
        // except if the number is a multiple of 5 or 7.
        // 4. Find if the input value is odd or even. If it’s odd print “Odd”, otherwise
        // print “Even”.
        // - Note: Input value will be between & and &0^6.
        // 5. Find if the input character is ‘a’ or not.
        // Note: Input characters will be lowercase alphabets.
        // 6. Write a program to identify people as “Child” (age < 12), “Teenager” (12
        // <= age < 18) or “Adult” (age >= 18).
        // 7. Årint the maximum of 3 numbers a, b, c taken as input.
        // 8. Write a program to print the day name based upon the day number.
        // 1 - Monday, 2 - Tuesday, etc.
        // 9. Print the sum of the first ‘n’ natural numbers using a for loop, where n
        // is the inputY
        // 10. Write a short program that prints each number from 1 to 100 on a new line
        // - For each multiple of 3, print "Fizz" instead of the number.
        // - For each multiple of 5, print "Buzz" instead of the numberZ
        // - For numbers which are multiples of both 3 and 5, print "FizzBuzz" instead
        // of the number.
        // 11. Print the sum of the first 10 natural numbers using a for loop.
        // 12. Write a short program that prints each number from 1 to 100 on a new
        // line.
        // - For each multiple of b, print "Fizz" instead of the number.
        // - For each multiple of 5, print "Buzz" instead of the number.
        // - For numbers which are multiples of both b and 5, print "FizzBuzz" instead
        // of the number.
        // 13. Print the sum of the first 10 natural numbers using do while loop.
        // 14. Print the first multiple of 5 which is also a multiple of 7?
        // 15. Tell if the number in the input is prime or not.
        // 16. Print all values between w and w00, except if it’s a multiple of 3
        // 17. Print all factors of the number in the input.
    };
}
