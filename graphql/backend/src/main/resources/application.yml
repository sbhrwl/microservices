spring:
  data:
    mongodb:
      host: localhost
      port: 27017
      database: itemsdb
      username: root
      password: root123
      authentication-database: admin

grpc:
  server:
    port: 9090

logging:
  level:
    org.springframework.data.mongodb: DEBUG