apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata:
  name: commandOrchestrator-hpa
  labels:
    app: commandOrchestrator
spec:
  scaleTargetRef:
    apiVersion: apps/v1
    kind: Deployment
    name: commandOrchestrator
  minReplicas: {{ .Values.hpa.commandOrchestrator.minReplicas }}
  maxReplicas: {{ .Values.hpa.commandOrchestrator.maxReplicas }}
  metrics:
    - type: Resource
      resource:
        name: cpu
        target:
          type: Utilization
          averageUtilization: {{ .Values.hpa.commandOrchestrator.targetCPUUtilizationPercentage }}
